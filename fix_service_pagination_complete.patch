diff --git a/FE/src/components/Service/Service.jsx b/FE/src/components/Service/Service.jsx
index abc123..def456 100644
--- a/FE/src/components/Service/Service.jsx
+++ b/FE/src/components/Service/Service.jsx
@@ -23,12 +23,12 @@ const Service = () => {
   const { control, handleSubmit, reset, formState: { errors } } = useForm();
   const navigate = useNavigate();
   const [page, setPage] = useState(1);
-  const [pageSize, setPageSize] = useState(10);
+  const [pageSize, setPageSize] = useState(12);
 
   const { data: services, isLoading: isLoadingServices } = useQuery({
     queryKey: ['userServices', page, pageSize],
     queryFn: async () => {
-      const { data } = await instance.get(`service?page=${page}&limit=${pageSize}`);
+      const { data } = await instance.get(`service?page=${page}&limit=${pageSize}&status=approved`);
       return data;
     },
   });
@@ -165,10 +165,10 @@ const Service = () => {
     setPageSize(pageSize);
   };
 
-  const docs = services?.data?.docs || [];
-  const totalDocs = services?.data?.totalDocs || 0;
-  const current = services?.data?.page || page;
-  const limit = services?.data?.limit || pageSize;
+  const docs = services?.data?.docs || [];
+  const totalDocs = services?.data?.totalDocs || 0;
+  const current = services?.data?.page || 1;
+  const limit = services?.data?.limit || pageSize;
 
   return (
     <div>
@@ -215,7 +215,7 @@ const Service = () => {
                       pageSize={limit}
                       total={totalDocs}
                       showSizeChanger
-                      pageSizeOptions={['3', '6', '10', '20']}
+                      pageSizeOptions={['6', '12', '24', '48']}
                       onChange={handleCardPagination}
                       showTotal={total => `Tổng số ${total} dịch vụ`}
                     />
@@ -232,7 +232,7 @@ const Service = () => {
                     pageSize: limit,
                     total: totalDocs,
                     showSizeChanger: true,
-                    pageSizeOptions: ['3', '6', '10', '20'],
+                    pageSizeOptions: ['10', '20', '50', '100'],
                     showTotal: (total) => `Tổng số ${total} dịch vụ`,
                   }}
                   onChange={handleTableChange}
